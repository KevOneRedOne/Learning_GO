package main

func main() {
	s := "Héllo"
	// println(recursivite.RecursiveStrLen([]rune(s)))
	// println(recursivite.RecursiveStrLen(s))

	for i := 0; i <= 5; i++ {
		toto := 2 * i
		tata := i * i
		println(toto + tata)
	}
}
